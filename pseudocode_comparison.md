# IGCSE 0478 与 AS 9618 伪代码：官方依据和本编辑器的实现

核对日期：2026年9月19日。本文区分“官方文件展示的写法”“考纲要求的范围”和“为可执行编辑器选择的规则”。指南中未列出某种写法，不能直接推断考试一律禁止它。

## 核对来源与适用年份

| 简称 | 官方文件 | 本文使用的印刷页码 |
|---|---|---|
| IGCSE | [Computer Science 0478 syllabus for 2026, 2027 and 2028](https://www.cambridgeinternational.org/Images/697167-2026-2028-syllabus.pdf)，版本6，2026年9月发布 | 第28页编程范围；第35–48页伪代码；第54页版本变更 |
| 9618 | [Pseudocode Guide for Teachers for 2027, 2028 and 2029](https://www.cambridgeinternational.org/Images/721401-2027-2029-pseudocode-guide.pdf)，版本1 | 第5–25页中与本应用相关的语法；更后章节另含 A Level 内容 |
| 旧年度对照 | [9618 Pseudocode Guide for Teachers for exams in 2026](https://www.cambridgeinternational.org/Images/697401-2026-pseudocode-guide-for-teachers.pdf) | 年份另行列出，不用它替代本应用当前9618依据 |

以下页码均为文件印刷页码，不是阅读器显示的 PDF 页序。IGCSE 第49页是考试指令词（Command words），不是新增伪代码语法。9618 指南涵盖整个 AS & A Level；“指南列出”不表示每项都属于 AS Paper 2 的考试范围。本文中的“AS 模式”指本应用已有的9618学习模式。

## 主要相同点与不同点

| 项目 | IGCSE 0478（2026–2028） | 9618指南（2027–2029） | 本应用处理 |
|---|---|---|---|
| 变量、赋值、比较 | `DECLARE Name : Type`；赋值 `←`，相等比较 `=`（第37–38、40页） | 相同（第8–9、16页） | 两种模式都要求使用前声明变量；不从省略声明的短例子推断变量类型 |
| 常量声明 | `CONSTANT Limit ← 10`；值为字面量（第37页） | `CONSTANT Limit = 10`；值为字面量（第8页） | 按模式区分声明符号，不自动把源码整体翻译成另一模式 |
| 标识符 | 字母、数字；不用下划线；推荐 PascalCase，也给出 `i`、`j` 等惯用短名；大小写不敏感（第36–37页） | 允许字母、数字和下划线，字母开头，大小写不敏感（第7页） | IGCSE 拒绝下划线，但不把首字母大写风格变成语法硬错误 |
| 基本类型 | `INTEGER REAL CHAR STRING BOOLEAN`（第36页） | 还列出 `DATE`（第7页） | IGCSE 保留五种基本类型；DATE 和记录等使用 AS 模式 |
| 一、二维数组 | 显式上下界；一般用下界1，不代表只能从1开始（第38–39页） | 同样显式边界，一般用下界1（第10–11页） | 保留声明边界；严格检查下标，不把所有数组强制改成0起始或1起始 |
| 整数除法 | 附录给出 `DIV(A, B)`、`MOD(A, B)`，参数为整数；DIV 丢弃小数部分（第40页） | 作为中缀算术运算符列出 `DIV`、`MOD`（第15页） | IGCSE 教学范例优先函数形式；保留中缀兼容，不声称函数形式是所有考试答案的唯一合法写法 |
| 乘方 | 明列 `^`（第39页） | 算术列表未列出 `^`（第15页） | `^` 属于本应用 IGCSE 模式；复杂幂表达式建议加括号 |
| 字符串长度与子串 | `LENGTH(S)`、`SUBSTRING(S, Start, Length)`；Start、Length 为正整数，一般以1表示首字符（第41页） | `LENGTH(S)`、`MID(S, Start, Length)`、`RIGHT(S, Count)`（第16–17页） | IGCSE 使用 SUBSTRING；AS 使用 MID/RIGHT；第三参数是长度，不是结束位置 |
| 大小写转换 | `UCASE`、`LCASE` 接受 STRING 或 CHAR，整字符串全部转换（第41页） | 给出的签名只接受 CHAR 并返回 CHAR（第17页） | 不把 AS 的单字符限制套到 IGCSE；IGCSE 保留输入的 STRING/CHAR 类型 |
| 连接与输出 | `OUTPUT` 可以用逗号输出多项；字符串例程列表未给出连接运算符（第39、41页） | 明列 `&` 连接字符串；OUTPUT 同样支持逗号（第15、17页） | IGCSE 范例用 `OUTPUT "Hello, ", Name`；本模式不接受 `&`，这是编辑器范围选择，不是对所有考题评分的判断 |
| 数值例程 | `ROUND(RealValue, Places)`；Places 为 INTEGER；`RANDOM()` 返回含0和1的实数（第41页） | `INT(Real)` 取整数部分；`RAND(Integer)` 返回从0开始、不含上界的实数（第17页） | 分离两组函数，不把 RANDOM 简单当成上界排除的 RAND(1) |
| WHILE | `WHILE Condition DO … ENDWHILE`（第45页） | `WHILE Condition … ENDWHILE`（第21页） | IGCSE 要求 DO；AS 保留原语法 |
| 其他控制结构 | IF/THEN/ELSE、CASE、FOR/NEXT、REPEAT/UNTIL；FOR 包含终值，STEP 可负（第42–45页） | 基本相同（第18–21页） | 两套模式共用控制结构语义；IGCSE `OTHERWISE Statement` 可不写冒号，也容许兼容冒号 |
| 无参数子程序 | 定义 `PROCEDURE Name`；调用 `CALL Name`；无参函数定义 `FUNCTION Name RETURNS Type`（第46–47页） | 无参数定义和调用示例使用括号（第22–23页） | IGCSE 支持官方无括号写法；也容许空括号。函数调用仍是表达式，不能加 CALL |
| 参数传递 | 定义参数类型并传入值；没有在此附录引入 BYVAL/BYREF 关键字（第46–48页） | 默认按值；过程可显式 BYVAL/BYREF（第22、24页） | IGCSE 参数在编辑器中按值传递；显式 BYVAL/BYREF 留在 AS 模式 |
| 文件处理 | READ、WRITE、OPENFILE、READFILE、WRITEFILE、CLOSEFILE（第48页） | 文本文件另有 APPEND 和 EOF；随机文件在后续章节（第25–27页） | IGCSE 模式支持 READ/WRITE；EOF 单独标为教学扩展；APPEND 留在 AS。两套编辑器都使用虚拟文本行，而非访问电脑任意文件 |

9618 第16页还明确说明：考试会提供所用的伪代码字符串操作函数。不同试题给出的具体定义仍需优先阅读，不能用本表替代题目条件。

## “三个参数”和“三层嵌套”应如何理解

当前 IGCSE 版本第28页规定：过程和函数可以有最多三个参数；不要求考生编写超过三层嵌套的语句。旧设计中“最多两个参数”的描述已经不适用于本次核对的版本。第54页变更记录说明版本4（2025年6月）更新了参数数目；版本6（2026年9月）更新了第41页 ROUND 的说明。

这两项描述的是该版考纲的要求范围，不是一个通用可执行伪代码解释器必须拒绝第四个参数或第四层结构的理由。因此，本应用不加“两参数”“三参数”或“三层嵌套”的考试专用硬限制；仍保留防止浏览器失去响应的通用深度、内存和执行限制。课堂核心示例遵守相关教学范围，另外提供一个三个参数的函数示例。

## 官方未完全定义的地方：本编辑器的明确约定

以下条目是实现约定，不应写成 Cambridge 保证的边界行为。

- **负数 DIV/MOD：**DIV 向零截断；MOD 的非零余数与被除数同号。例如 `DIV(-7, 3) = -2`，`MOD(-7, 3) = -1`。零除数报告错误。课堂主例用正整数。
- **ROUND：**返回 REAL；整数参数可安全扩大为 REAL。恰好位于一半时远离零舍入；允许负小数位以舍入到十位、百位等。第41页未具体规定平局策略或负位数语义。浮点数仍受有限精度影响，不应据此训练与题目无关的极端二进制舍入技巧。
- **幂：**`^` 从右结合、优先于一元负号；显式括号更清楚。INTEGER 底数配合静态可确定的非负 INTEGER 指数返回 INTEGER；其他数值幂返回 REAL。数值无效或越界会报告错误。该返回类型策略及复杂优先级细节是编辑器约定。
- **RANDOM：**使用包含0和1的均匀32位离散网格模拟随机 REAL。随机结果不应使用某一个固定样本作为课堂预期输出；示例只检查值是否在范围内。
- **SUBSTRING 与字符：**首字符位置为1；起点和长度须为正整数；整个请求区间须在文本内。按 Unicode 码点计数。CHAR 大小写转换若扩展成多个字符会报错，不暗中改为 STRING。
- **文件：**每次 READFILE 读取一行 STRING，WRITEFILE 写一行；保留空白行。读写对象是本应用的虚拟文件。IGCSE 官方表述为“数据项”，此处实现的是适合本课程的文本行模型。
- **EOF：**IGCSE 第48页未列出 EOF。为了让学生读取未知行数，编辑器保留这一明确标注的教学扩展；按附录列出的最小语法做题时，可使用题目提供的记录数或其他终止条件。未列出不等于考试禁止。
- **可执行约束：**声明后才能使用变量，未赋值读取会报错；类型、数组边界及文件状态会检查。默认执行上限为1,000,000步、10,000行输出；追踪保留5,000项，达到追踪上限不等于程序立即终止。另有存储、字符串和调用深度保护，均不是考试知识点的限制。

格式化器保留注释和字符串内容，使用应用统一的四空格缩进。IGCSE 印刷指南还区分 THEN/ELSE 等续行的两空格缩进；编辑器不把这一排版约定变成语义规则。

## 课堂内容如何对应

原有16个核心主题现有 IGCSE、AS、Python 三个独立源码版本；原有6个 Python 扩展保持不变。另加3个 IGCSE 小例：整字符串大小写与 SUBSTRING；常量、DIV/MOD、幂、ROUND/RANDOM；无参数过程和三个参数函数。

- 第8课 IGCSE 使用 SUBSTRING 取中间和末尾片段，AS 仍使用 MID/RIGHT。
- 第15课 IGCSE 在标题、说明和源码注释中注明 EOF 教学扩展；与另外两种模式使用相同虚构文件，覆盖空白行和未知行数。
- 第16课 IGCSE 改为一次 WRITE 会话写入两行，再按已知行数核对。AS/Python 仍演示追加；三种版本得到相同文件内容，但 IGCSE 版本不声称使用了 APPEND。标题和教学目标按当前模式显示。
- 问候语和过程例子使用 IGCSE OUTPUT 逗号列表，避免借用 AS 的 `&`。

验证结果：19个 IGCSE 示例的完整输入、输出、虚拟文件结果，以及11个边界场景，共30项通过。边界包括等级阈值、0和100、立即输入哨兵、查无目标、空文件及没有末尾换行的最后一行。检查同时确认原22个课堂条目的 AS/Python 内容未被改写。此结果是本地解释器验证，不等同于浏览器集成测试或 Cambridge 官方认证。
